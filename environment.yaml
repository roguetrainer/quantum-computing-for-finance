name: quantum-finance
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip>=21.0
  
  # Numerical computing
  - numpy>=1.21.0
  - scipy>=1.7.0
  
  # Data manipulation
  - pandas>=1.3.0
  
  # Visualization
  - matplotlib>=3.4.0
  - seaborn>=0.11.0
  
  # Jupyter
  - jupyter>=1.0.0
  - ipykernel>=6.0.0
  - ipywidgets>=7.6.0
  - notebook>=6.4.0
  
  # Machine learning
  - scikit-learn>=1.0.0
  
  # Utilities
  - tqdm>=4.62.0
  
  # Install via pip (not available in conda)
  - pip:
    - pennylane>=0.33.0
    - pennylane-lightning>=0.33.0
    - tensornetwork>=0.4.6
    - plotly>=5.0.0